import Ember from 'ember';

export default Ember.Component.extend({
  tagName    : 'nav',
  classNames : [ 'top-bar' ],

  actions: {
    search() {
      // do something useful here ...
    }
  }
});
